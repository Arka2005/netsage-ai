# Python
__pycache__/
*.py[cod]
.venv/
venv/
# a venv created in the repo root rather than a subdirectory (python -m venv .)
/Lib/
/Scripts/
/pyvenv.cfg
.pytest_cache/
*.egg-info/

# Secrets
.env
.env.*

# Generated artifacts (keep the directory, ignore the noise)
artifacts/runs/*.jsonl
artifacts/dashboard.html

# Packet Tracer working files
*.pkt.bak
*.pka

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# One-time setup helper (delete after running)
init-repo.bat

# Spec docs — kept locally, not versioned
docs/
